# Per-Page Hero Images

Reserved folder for any future per-page hero photos (e.g. a splash photo
on the Pricing or Community page if we want to add one later).

Currently unused. Pages today use either the slideshow (home) or
gradient/sparkle backgrounds (everywhere else).
